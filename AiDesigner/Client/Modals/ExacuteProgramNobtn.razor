@using NodeBaseApi.Version2;
@using Microsoft.AspNetCore.Components.Authorization
@using System.Security.Claims

<div class="@($"fixed top-0 left-0 right-0 z-50 w-full p-4 overflow-x-hidden overflow-y-auto md:inset-0 h-[calc(100%-1rem)] max-h-full {(IsModalOpen ? "block" : "hidden")}")" style="background-color: rgba(0, 0, 0, 0.5);">
    <div class="relative w-full max-w-2xl max-h-full mx-auto">
        <!-- Modal content -->
        <div class="relative bg-white rounded-lg shadow dark:bg-gray-800">
            <!-- Modal header -->
            <div class="flex items-start justify-between p-4 border-b rounded-t dark:border-gray-600 dark:bg-gray-800">
                <h3 class="text-xl font-semibold text-gray-900 dark:text-white">
                    Exacute Program
                </h3>
                <button type="button" class="text-gray-400 bg-transparent hover:bg-gray-200 hover:text-gray-900 rounded-lg text-sm p-1.5 ml-auto inline-flex items-center dark:hover:bg-gray-600 dark:hover:text-white" @onclick="Close">
                    <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd"></path></svg>
                </button>
            </div>
            <!-- Modal body -->
            <div class="p-6 space-y-6 dark:text-gray-300 dark:bg-gray-600 dark:text-white">
                <div class="flex space-x-4">
                    <div class="w-1/2 border-r border-gray-300 dark:border-gray-500">
                        @if (AP is CustomProgram customProgram)
                        {
                            <h3 class="text-lg font-semibold">Inputs:</h3>
                            <ul>
                                @if (customProgram != null)
                                {
                                    @foreach (var input in customProgram.ProgramStructure.ProgramStart)
                                    {
                                        <li>@input.Name (@input.Type)</li>
                                    }
                                }
                            </ul>
                        }
                        else if (AP is CustomBlockProgram customBlockProgram)
                        {
                            <h3 class="text-lg font-semibold">Inputs:</h3>
                            <ul>
                                @if (customBlockProgram != null)
                                {
                                    @foreach (var input in customBlockProgram.ProgramStructure.ProgramStart)
                                    {
                                        <li>@input.Name (@input.Type)</li>
                                    }
                                }
                            </ul>
                        }
                    </div>
                    <div class="w-1/2">
                        @if (AP is CustomProgram customProgram2)
                        {
                            <h3 class="text-lg font-semibold">Inputs:</h3>
                            <ul>
                                @foreach (var input in customProgram2.ProgramStructure.ProgramEnd)
                                {
                                    <li>@input.Name (@input.Type)</li>
                                }
                            </ul>
                        }
                        else if (AP is CustomBlockProgram customBlockProgram2)
                        {
                            <h3 class="text-lg font-semibold">Inputs:</h3>
                            <ul>
                                @foreach (var input in customBlockProgram2.ProgramStructure.ProgramEnd)
                                {
                                    <li>@input.Name (@input.Type)</li>
                                }
                            </ul>
                        }
                    </div>
                </div>
            </div>
            <!-- Modal footer -->
            <div class="flex items-center justify-center p-6 space-x-2 border-t border-gray-200 rounded-b dark:border-gray-600 dark:bg-gray-600">
                <button type="button" class="btn btn-primary px-4 py-2 text-sm font-semibold text-white bg-green-600 hover:bg-green-700 border-0" @onclick="@HandleNextButtonClick">Exacute</button>
            </div>
        </div>
    </div>
</div>



@code {
    private bool IsModalOpen { get; set; }

    [Parameter]
    public ProgramObject AP { get; set; }

    public void Open()
    {
        IsModalOpen = true;
        StateHasChanged();
    }

    public void Close()
    {
        IsModalOpen = false;
        StateHasChanged();
    }

    private async Task HandleNextButtonClick()
    {
        
    }
}